# DESIGN.ko.md — 비전 실현 가능성 모니터 설계서

**작성자**: 아영 (Ayoung) · **최종 수정일**: 2026-05-25

> 운영 및 개발에 필요한 상세 컨텍스트는 [CLAUDE.md](./CLAUDE.md)를 보면 된다. 현재 진행 중인 마일스톤 작업은 [docs/tasks/current.md](./docs/tasks/current.md), Phase 4 데이터 파이프라인 알고리즘 설계는 [docs/architecture/composition.md](./docs/architecture/composition.md), 에이전트 목록은 [docs/agent-capabilities.md](./docs/agent-capabilities.md)를 참고하자.
>
> 이 문서는 장기적으로 유지되는 핵심 전략적 설계(durable strategic content)만 담고 있다. 예전 로드맵, 비즈니스 모델, sitemap, 개별 주식 등 은퇴한 섹션들은 깃 히스토리에 안전하게 보존되어 있다. Phase 3 피봇(Sector Simulator → Vision Feasibility Monitor)의 과거 맥락이 궁금하다면 [docs/archive/pivot.md](./docs/archive/pivot.md)를 읽어보면 된다.

---

## 1. 비전 (Vision)

### 1.1 한 줄 요약

> **우주 데이터센터, 핵융합 발전, 상온 초전도체 등 세상을 흔들 원대한 기술 비전을 하나 골라보자. 우리는 이 기술을 구현하는 데 필요한 모든 세부 능력(Capability)과 기술을 자극하는 외부 증거(Signal)들을 추적하여, 단 5초 만에 실현 가능성 수준을 직관적으로 파악할 수 있는 종합 점수로 요약해 보여준다.**

### 1.2 우리가 풀려는 문제 (The problem)

기술 실현 가능성에 대한 양질의 정보가 너무 사방에 흩어져 있다.
- **기존 분석 리포트의 한계**: 가트너 하이프 사이클(Gartner Hype Cycle)이나 MIT 테크 리뷰 10대 유망 기술 같은 리포트들은 발간되는 즉시 낡은 정보가 된다. 분기나 연 단위로 무거운 PDF 형태로만 제공되기 때문이다.
- **결론 도출 과정의 불투명성**: "AI 전용 메모리 수요가 폭증할 것"이라는 식의 뜬구름 잡는 결론은 많지만, 그 결론을 뒷받침하는 인과 구조, 가정, 원본 출처가 너무 불투명하다.
- **가정치 튜닝 불가 (What-If)**: "우주선 발사 비용이 50% 더 저렴해진다면 어떻게 될까?" 같은 조건 변화에 따른 시나리오를 사용자가 즉각 시뮬레이션해 볼 수 없다.
- **전문 분석의 높은 비용**: 새로운 기술 비전(예: 핵융합, BCI 등)을 하나 분석하려 할 때마다 매번 수개월씩 걸리는 값비싼 전문가 리서치 비용을 치러야 한다.

### 1.3 해결책 (The solution)

1. 각 비전(Vision)을 약 10가지 안팎의 핵심 세부 능력(**Capability**: 기술, 경제, 규제, 공급망 요건)으로 잘게 쪼갠다.
2. 매일 새롭게 쏟아지는 외부 증거(**Signal**: arXiv 논문, 특허, 실시간 뉴스, 정부 보고서 등)가 AI 에이전트(Extractor)를 거치며 Capability 평점 영향력 데이터로 번역 및 자동 업데이트된다.
3. **FeasibilityIndex** 엔진이 Bayesian 집계 연산과 리비히 최소량 법칙(Liebig binding constraint)을 결합하여 비전 단위의 0~100 종합 점수와 달성 예상 년수(ETA) 분포를 명확히 뽑아낸다.
4. **Actor** 레이어가 각 Capability를 실질적으로 개발하고 이끌어가는 글로벌 기업, 핵심 연구소, 정부 기관을 정밀 추적한다.
5. 도커 기반으로 늘 살아 움직이는 **크롤러 서비스**가 실시간 인터넷 데이터에서 Signal, Risk, Economics 지표들을 흡수하며, 새로운 개체가 발견되면 **`@feasibility_bot`** 에이전트가 직접 커뮤니티 제안(`CommunityProposal`)을 발의해 집단지성 투표 큐레이션에 올린다.
6. 시뮬레이터(**Playground**) 탭은 사용자가 슬라이더를 직접 밀고 당기며 기술과 미래 산업의 병목을 직관적으로 학습하고 튜닝해 볼 수 있는 도구다.

### 1.4 경쟁사 대비 우리의 강점 (Differentiator)

| 기존 솔루션 | 한계점 | 우리의 강점 |
| :--- | :--- | :--- |
| **MIT Tech Review 유망 기술** | 연 1회 PDF 발간, 정적임 | 매일 실시간 업데이트되며 모든 숫자가 출처로 증빙됨 |
| **가트너 하이프 사이클** | 분기 단위 발간, 출처가 투명하지 않음 | 대시보드의 모든 평점 숫자가 원본 증거(시그널)까지 원클릭 추적됨 |
| **CB Insights** | 개별 기업 중심 분석, 기술 그 자체에 집중하지 않음 | 거대 기술 비전(Vision-centric) 중심의 아키텍처 구조 |
| **Polymarket (예측 시장)** | 게임판 형태, 인과 관계 구조가 없음 | 인과 관계를 지닌 완전한 가치 사슬 구조 (Capability 트리 ↔ Signals) |
| **학술 논문 / 특허 DB** | 정제되지 않은 원본 데이터, 종합 지표 부재 | 쏟아지는 논문과 특허 시그널들이 점수 변동량으로 정밀 합산됨 |

---

## 2. 타깃 사용자 및 페르소나 (Users & personas)

우리가 가장 중요하게 생각하는 타깃 사용자 우선순위는 다음과 같다.

| 우선순위 | 페르소나 | 핵심 과업 (Core job) | 주요 활용 방식 |
| :--- | :--- | :--- | :--- |
| **1** | **딥테크 VC 파트너** | 투자 가치 증빙 및 가설 검증 | "이 우주 데이터센터 기업에 200억을 태워도 안전할까?" -> 대시보드 Hero 5초 스캔 및 세부 시그널 출처 추적 |
| **2** | **국가 R&D 정책 기획자** | 연구 예산 및 펀딩 분배 | "어느 기술 도메인에 예산을 먼저 투입해야 병목이 뚫릴까?" -> 5개 이상의 비전을 나란히 비교 분석 |
| **3** | **대기업 미래사업본부 전략가** | 5~10년 장기 비즈니스 포지셔닝 | "우리 기업의 다음 10년 먹거리는 어디에 배치해야 하나?" -> 세부 Capability 카드를 파고들어 플레이어 구도 분석 |
| **4** | **호기심 넘치는 엔지니어/창업가** | "내 인생을 바쳐 도전해 볼 만한 영역인가?" 판정 | 세부 능력이 시간 경과에 따라 발전하는 피드를 스크롤하며 트렌드 포착 |
| **5** | **테크 저널리스트** | 신뢰도 높은 정보 인용 | 팩트 기반으로 잘 정리된 소스 시그널을 긁어가서 기사에 대시보드 그래프 임베딩 |

*도메인 기여자(제안서 제출, 에이전트 제안에 투표하는 유저)는 4번 페르소나를 제외한 모든 집단이 해당된다.*

---

## 3. 핵심 추상화 구조 (Core abstractions)

```
Vision (is_vision_eligible=true 조건의 Sectors 테이블 행)
  ├─ Capability   기술 / 경제 / 규제 / 공급망 평가 차원 (CapabilityScore 시계열 스냅샷)
  ├─ Actor        글ローバル 스타트업/연구소/정부 기관 (Capability별 역할 매핑)
  ├─ Signal       논문 / 특허 / 뉴스 / 공시 / 정부보고서 / 데이터셋 (외부 증거)
  ├─ Risk         정치 / 법률 / 공급망 / 안전 / 환경 / 금융 / 사회적 저해요소
  └─ Feasibility  비전 단위 종합 달성률 점수 및 예상 소요년수(ETA) 오차 범위 (P10-median-P90)
```

*   **수집 파이프라인 흐름**:
    외부 데이터 (Signals) 유입 ➔ **SignalExtractor 에이전트** (차원별 영향력 가감치 판정 & Actor 자동 감지) ➔ **ScoreUpdater 에이전트** (누적 평점 분석) ➔ **CapabilityScore 시계열 적재** ➔ **VisionFeasibility 종합 집계** (리비히 최소량 완충 규칙 적용).

---

## 4. 구현 기능 목록 (Features)

기능의 구체적인 상태가 아닌, 시스템이 제공하는 **장기적 기능 모델**에 대한 상세 내역이다.

*   **F1. 비전 생성 에이전트 (`Vision Builder`)**: 한 줄의 자연어 질문을 던지면 검증 게이트를 타고 해외 웹 검색, 능력 분해, 의존성 설정을 마쳐 단일 데이터베이스 트랜잭션으로 자동 빌드한다.
*   **F2. 실시간 정보 수집 (`Signal Ingest`)**: arXiv, USPTO, 뉴스 RSS 등 다이렉트 어댑터와 복합적인 리서치 요약(Grounded Gemini Deep Research)을 융합하여 시그널을 실시간으로 적재한다.
*   **F3. 비전 대시보드 (`The Hero`)**: 5초 만에 모든 상황을 이해할 수 있는 Feasibility 게이지, 궤적, ETA 윈도우, 8가지 탭과 함께 실시간 파이프라인 상태를 비쳐주는 **Live Pulse** 위젯을 제공한다.
*   **F4. 시뮬레이터 (`Playground`)**: 사용자가 드라이버 슬라이더 수치를 직접 움직이면 브라우저 단에서 집계 공식이 즉각 반응하여 What-If 가능성을 차트로 그려낸다.
*   **F5. 출처 추적 및 증빙 (`Provenance`)**: 화면 상의 모든 점수와 수치가 원본 데이터(URL)로 즉각 역추적된다. AI가 생성한 모든 제안 및 초안에도 원본 출처(`source_ref`) 기재를 Pydantic 레벨에서 강제한다.
*   **F6. 집단지성 커뮤니티 (`Community`)**: 7가지 타입의 발의서 제출, 투표에 따른 자동 병합 시스템, 난이도 자동 판정 기반 주가 예측(`PredictionV2`)과 유저 팔로우 그래프를 제공한다.
*   **F7. 에이전트 자동 제안 (`Bot-authored proposals`)**: `@feasibility_bot` 에이전트가 실시간 크롤링 중 새로 감지된 액터나 요건들을 분석하여 직접 `CommunityProposal`을 작성 및 발의한다.
*   **F8. 관리자 크롤러 코크핏 (`Admin Crawler Cockpit`)**: `/admin/queue` 전용 뷰 및 `/admin` 조율 도구를 통해 크롤러 헬스 상태와 예산 소모를 모니터링하고, 비전별 스케줄 및 윈도우/한도 설정을 실시간 튜닝한다.
*   **F9. 시각화 차트 팩 (`Vision Visualization pack`)**: Capability 방사형 레이더, 달성도 타임라인, 위험 히트맵 등 모든 다크/라이트 테마에 완벽 대응하는 인포그래픽 차트를 제공한다.
*   **F10. 다국어 및 테마 (`i18n + theme`)**: 한글 기본 및 영문 패리티를 지원하며, 기기 간 언어/테마 쿠키를 유저 테이블에 동기화한다. 한국어의 경우 번역투를 지양하고 친근하고 자연스러운 **존댓말** 구사를 지향한다.

---

## 5. 에이전트 토폴로지 (Agent topology)

에이전트 워크플로우가 사용하는 LLM 등급과 트리거 시점 명세다.

| 워크플로우 이름 | 사용 LLM 등급 (Gemini) | 언제 구동되는가? (Trigger) |
| :--- | :--- | :--- |
| **VisionResearch** | balanced (gemini-3.5-flash) | 관리자가 새로운 비전을 발의했을 때 검색 수행 |
| **VisionDecomposition** | deep (gemini-3.1-pro-preview) | 리서치 단계가 끝나고 세부 능력 10대 요건을 쪼갤 때 |
| **CapabilityToDriver** | balanced | 요건 분해 후 Playground 슬라이더 수식 변수를 맵핑할 때 |
| **CapabilityDependencies** | deep | 요건 간의 선행 DAG 의존성 그래프를 형성할 때 |
| **CapabilityScoringCode** | deep | 요건 점수를 계산할 Python 함수 코드를 직접 짤 때 |
| **CodeReview** | deep | AI가 짠 계산 코드가 안전하고 올바른지 검수할 때 |
| **SignalExtractor** | fast (gemini-3.1-flash-lite) | 파이프라인에 새 시그널이 들어올 때마다 즉시 델타 분석 (하루 100~1000회) |
| **ScoreUpdater** | balanced | 특정 요건에 매핑된 시그널들을 모아 점수를 최종 갱신할 때 |
| **DeepResearch** | pro-preview (deep tier, grounded) | 데일리 종합 다이제스트 리포트를 작성할 때 |
| **EntityDetector** | balanced | 수집된 논문/뉴스 뭉치에서 새로운 액터나 능력을 감지할 때 |
| **ProposalDrafter** | balanced | 감지된 개체의 신뢰도가 차올라 봇이 제안서를 자동 작문할 때 |

---

## 6. 시스템 서비스 맵 (System architecture)

*   `data-pipeline`: arXiv, 특허, 뉴스 API, 금융 페이지의 직접 수집 및 실시간 Feasibility 재계산 배치 수행. 어드민 SQLAdmin 화면 마운트.
*   `simulation-service`: 4차원 점수 집계 및 리비히 최소량 병목 연산, ETA 추론 엔진 담당.
*   `agent-orchestration`: 비전 자동 생성 전 과정 및 점수 가감 extractor/updater 워크플로우 구동.
*   `crawler` (실시간 봇): Gemini Deep Research 연동 수집 봇 및 `@feasibility_bot` 발의 에이전트. Redis 스트림 큐 기반의 Docker 상시 구동 서비스.

---

## 7. 비기능적 요구사항 (Non-functional requirements)

### 7.1 성능 버젯 목표 (P95 성능 기준)
- 대시보드 Hero 페이지 TTFB (첫 바이트 로드): **500ms 미만**
- 단일 DB 라운드트립 API 로드: **200ms 미만**
- 요건 상세 정보 화면 로드: **300ms 미만**
- 슬라이더 조절 시뮬레이션 갱신 (캐시 적용 시): **800ms 미만**
- LLM 응답 시작 시간 (TTFT): **1s 미만**

### 7.2 경제적 비용 통제 (Cost Economics)
- **유저당 월평균 LLM 비용**: **$30 미만** (최종 프로 요금제 마진을 위한 가이드라인)
- **비용 절감 기법**:
  1. **Gemini 프롬프트 캐싱**: 반복되는 에이전트 시스템 프롬프트에 `cache_control: ephemeral` 명시.
  2. **시뮬레이션 결과 캐싱**: 입력값과 드라이버 딕셔너리 해시값 기준으로 결과 캐싱.
  3. **영리한 모델 라우팅**: 단순 분류는 제일 값싼 `fast` 등급을 쓰고, 정밀 추론에만 `balanced`와 `deep` 등급을 단계별로 에스컬레이션하여 사용.

### 7.3 보안 가이드라인 (Security)
- AI가 작성한 코드는 무조건 격리된 샌드박스 환경(E2B/Modal)에서만 실행한다.
- 모든 어드민 페이지 조작 및 API 호출은 감사 로그(`AuditLog`)에 무조건 기록한다.
- 모든 Vertex AI 인증서 및 서비스 키는 절대 깃허브에 올리지 않고 Doppler 및 암호화된 `/secrets` 디렉토리에만 보관한다.

---

## 8. UX 디자인 철학 (UX principles)

1. **글보다 시각화 (Show, don't tell)**: 텍스트 줄글보다 부드러운 인터랙티브 그래프와 게이지가 먼저다.
2. **점진적 정보 노출 (Progressive disclosure)**: 첫 화면은 극도의 가독성을 자랑하는 한 줄 결론 점수만 보여주고, 클릭 시에만 깊은 디테일을 펼친다.
3. **정직한 오차 범위 표시**: 점수의 완벽함을 억지로 포장하지 않고 P10-P90 오차 범위 밴드를 솔직하게 노출하여 신뢰를 얻는다.
4. **모든 수치의 증빙 투명성**: 화면 상의 모든 점수는 클릭 한 번으로 원본 뉴스/논문 출처 팝업으로 이어져야 한다.
5. **모바일 및 다크 모드 퍼스트**: 고밀도의 기술 데이터 분석을 눈의 피로 없이 훑을 수 있도록 미려하고 시크한 다크 모드를 디폴트로 제공한다.
6. **자연스러운 언어 구사**: 영문은 편안한 대화형 어조, 국문은 기계적인 직역투를 철저히 차단하고 친근한 **경어체(존댓말)**를 균형 있게 구사한다.

---

## 9. 위험 요소 및 대처 방안 (Risks & mitigations)

*   **AI 에이전트의 기술 분해 뇌절 (Hallucination)**: 생성 과정에서 논문/특허 URL 증빙을 Pydantic 레벨에서 강제하고, 최종적으로 데이터 커밋 전 어드민이 손쉽게 검수할 수 있는 승인 대기 단계를 설계했다.
*   **시그널 노이즈에 따른 평점의 급격한 널뛰기**: 단일 시그널의 영향력 임계치를 통제하고, 여러 시그널이 뭉쳐서 누적 검증되었을 때만 점수 updater 에이전트가 동요하도록 가중치 계산을 보완했다.
*   **봇의 무분별한 스팸성 발의 (Proposal flooding)**: 최소 7일 이내에 서로 다른 2개 이상의 고유한 출처에서 시그널이 교차 감지되었을 때만 봇이 정식 발의서를 제출할 수 있도록 교차 검증 트리거를 설계했다.
